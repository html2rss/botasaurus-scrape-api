[tool.ruff]
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
ignore = [
    "E501",  # formatter owns line width
    "SIM105",  # keep try/except-pass in isolation/cleanup and best-effort metadata
]
